# Git shorthands.
# Vendored from omarchy-zsh.
# See also functions/worktrees.zsh, which defines `ga` and `gd`.

alias g='git'
alias gcm='git commit -m'
alias gcam='git commit -a -m'
alias gcad='git commit -a --amend'
