# Environment defaults.
# Vendored from omarchy-zsh's `envs`, minus its PATH line — PATH is owned by
# env/path.sh, which prepends rather than appends.

export EDITOR="nvim"
export SUDO_EDITOR="$EDITOR"
export BAT_THEME=ansi
