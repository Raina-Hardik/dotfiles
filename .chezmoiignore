# Exclude sensitive files
.git/
api-keys.zsh
api-keys.sh

# nvim plugin lockfile - machine-specific, do not track
.config/nvim/lazy-lock.json

# Package manifests live in the repo for rebuilds; they are not dotfiles
# and must not be written into $HOME.
packages/

# Repo documentation, matched at the root only (chezmoi's ignore patterns
# aren't recursive like gitignore, so no leading slash is needed here, and
# newer chezmoi rejects a leading slash as an invalid path).
README.md
MANUAL-STEPS.md
